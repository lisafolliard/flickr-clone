<%= form_for(Tag.new, remote: true) do |f| %>
  <%= f.hidden_field(:photo_id, value: photo.id) %>
  <%= f.collection_select :user_id, User.all, :id, :name %>
  <%= f.submit "Finished Tagging" %>
<% end %>
